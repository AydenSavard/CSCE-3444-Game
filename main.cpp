#include <iostream>
#include <string>
#include "boardSetup.h"
using namespace std;

int main(){

    string bSize = "5x6";//format for the size of the board
    boardSetup(bSize);

    return 1;
}